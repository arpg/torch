#include <gtest/gtest.h>
#include <torch/Torch.h>

using namespace torch;

TEST(Problem, AlbedoDerivatives)
{
  // Problem problem;
  // problem.ComputeAlbedoDerivatives();
  // std::shared_ptr<SparseMatrix> jacobian;
  // jacobian = problem.GetAlbedoJacobian(0);
}